# External P1 reference imports

No Postshot or legacy COLMAP+Brush reference was present on 2026-07-12.
These directories are import-only placeholders; generated candidate results
must never be copied here or relabeled as references.

- `postshot/`: Postshot project/export and fixed-camera renders.
- `legacy_brush/`: old COLMAP+Brush Gaussian export and matching renders.

After importing, fill each directory's `reference.manifest.json` with source
hashes, tool version, camera mapping, and license/usage notes before comparison.
